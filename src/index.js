export default {
  CELLINDEX: require('formula-cellindex'),
  INDEX2COL: require('formula-index2col'),
  INDEX2ROW: require('formula-index2row'),
  MAX_COLS: require('formula-constants').MAX_COLS,
  MAX_ROWS: require('formula-constants').MAX_ROWS
}
